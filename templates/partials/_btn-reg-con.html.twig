{% if app.user %}
    {% if app.user.internetUsers %}
        {# <a href="{{ path('app_register', {status: 'providers'}) }}">Devenez prestataire</a> #}
    {% endif %}
{% else %}
    <a href="{{ path('app_login') }}">Connexion</a>
{% endif %}